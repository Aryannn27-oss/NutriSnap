# Plan 03-02: Gemini API Integration & AI Review Screen

Implement server-side Gemini 2.5-flash endpoint and the interactive review/edit flow.

## Proposed Changes
- Create [route.ts](file:///e:/antigravity/src/app/api/analyze/route.ts) to query Gemini 2.5-flash using `responseSchema` with confidence scores.
- Build the review form UI on [upload/page.tsx](file:///e:/antigravity/src/app/upload/page.tsx) with inline editors, overall confidence display, "Analyze Again" option, and dynamic macro recalculations.
- Store results under `users/{uid}/meals` subcollection in Firestore with original AI payload and timestamps.

## Verification
- Verify compilation succeeds with `npm run build`.
- Confirm saved records appear on the user profile with correct parameters.
