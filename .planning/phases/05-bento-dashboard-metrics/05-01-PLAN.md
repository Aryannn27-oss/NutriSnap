# Plan 05-01: Create daily log query hook and macros arithmetic helper

Establish database connections for today's logs.

## Proposed Changes
- Set up a query on the Firestore `users/{uid}/meals` subcollection filtering by `date == localDateStr` inside [dashboard/page.tsx](file:///e:/antigravity/src/app/dashboard/page.tsx).
- Add real-time snapshot event handling using `onSnapshot` to automatically refresh meal logs in state.
- Sum total calories, protein, carbs, and fats from retrieved records.

## Verification
- Confirm that adding a meal in Firestore updates total calculations in dashboard state immediately.
