# Plan 05-02: Render live progress metrics cards on dashboard

Build the live progress metrics cards and weekly chart interface.

## Proposed Changes
- Render macro progress bars (Calories, Protein, Carbs, Fats) on [dashboard/page.tsx](file:///e:/antigravity/src/app/dashboard/page.tsx) reflecting aggregated sums compared to `profileData` limits.
- Render dynamic weekly chart with highlights on current day of the week, initializing other days to 0 kcal.
- Display visual list of today's logged meals with thumbnail images and calorie details.
- Render a redirect empty state overlay if `profileData` targets are not configured.

## Verification
- Verify progress bars reflect modifications immediately.
- Confirm empty state appears on a new account without calorie targets.
