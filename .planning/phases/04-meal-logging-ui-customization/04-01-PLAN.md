# Plan 04-01: Write Firestore CRUD functions for meals

Implement core database write operations for meals.

## Proposed Changes
- Save approved meal logs under the `users/{uid}/meals` subcollection in [upload/page.tsx](file:///e:/antigravity/src/app/upload/page.tsx).
- Store both the original AI response payload and the final user-confirmed values.
- Enforce timestamps using Firebase `serverTimestamp()` for `createdAt` and `updatedAt`.
- Index dates using `YYYY-MM-DD` in user's local timezone.

## Verification
- Verify that clicking "Save Meal" writes a new document in Firestore with full macros, names, image URL, timestamps, and AI metadata.
