# Plan 04-02: Implement meal item edit sheet / detail drawer UI

Create inline editors on the Meal Log page and upload review screen.

## Proposed Changes
- In [upload/page.tsx](file:///e:/antigravity/src/app/upload/page.tsx), render an inline edit interface allowing portion size, food name, calories, and macro overrides.
- In [meals/page.tsx](file:///e:/antigravity/src/app/meals/page.tsx), add an "Edit" button to load meal log data into form fields for inline modification, writing updates back with `updatedAt: serverTimestamp()`.

## Verification
- Confirm that editing macros updates visual totals immediately.
- Confirm edits to saved meal logs persist to Firestore.
